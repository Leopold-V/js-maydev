export type userType = {
    userId: string,
    username: string,
    email: string,
    avatar: string
}

export type questionType = {
    id: string,
    authorId: string,
    content: string,
    date: string
}